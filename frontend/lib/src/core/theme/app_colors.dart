import 'package:flutter/material.dart';

/// Paleta de cores centralizada do app.
class AppColors {
  AppColors._();

  // Cores principais
  static const Color vermelho = Color(0xFFE53935); // ação / botão principal
  static const Color verde = Color(0xFF43A047); // sucesso / confirmado
  static const Color branco = Color(0xFFFFFFFF); // fundo

  // Textos
  static const Color pretoForte = Color(0xFF212121); // título
  static const Color cinzaEscuro = Color(0xFF616161); // texto normal
  static const Color cinzaClaro = Color(0xFF9E9E9E); // texto secundário

  // Apoio
  static const Color erro = Color(0xFFD32F2F);
  static const Color divisor = Color(0xFFEEEEEE);
  static const Color fundoCampo = Color(0xFFF5F5F5);
}
